Machine Learning Series:
    ML - MAchine learning is all about teaching computers to learn from data - just like we learn from experience

    supervised learning: in this we give input and out variable and we train the model base don that data and after training the model we can predict tehe outut for new input data

    Note - 99% machine learning applications works on supervised learning.

    supervised learning me ham machine ko ek data provide karte hai jisme 2 chize hoti hai ek input variable and dusra output variable then ham model ko train karte hai us data par and model kuch patterns build karta hai after training the model ham new data provide karte hai as input and is input data ke liye output predict karwate hai.

    in supervised learning we have input and output variables both

    when we are talking about supervised learning there are usually input and output variables are given and then we train our model based on the data that is provided after that we will predict the output variable after training the model.

    *** Unsupervised learning:
    example - searching virat kohli on google and google suggesting the related images, links, blogs, etc related to virat kohli itself so This is the unsupervised learning.

    ek works like makes a group of similar thing or related thing 
    so it makes a cluster and it representing the virat kohli. 

    so it finds pattern, group similar items, reducing complexity. so all it comes under unsupervised learning.

    Note: usually unsupervised leanring ka use hota hai supervised learning ke model train karne me.

    unsupervised learning me only input data hota hai. There is no output. and is only input par ham apne model ko train karte hai.

    isme ham kuch predict nahi karte hai.
    applications are reccomendation like: youtube and netflix recommendation.

    *** Reinforcement learning:
    reward good or correct result and punish bad or incorrect result or behavior.
    application:  


    
---------------
ham data privide karte hai and us data par machine ko train karte hai and after that machine ab vo machine output provide kar paati hai

mai bahit sara data de dunga ml ko and us data ke base par vo machine khud se pattern banane lagegi and jab vo pattern banane lagegi then pattern banane ke baad ab vo machine predict kar paayegi
---------------

****** EDA:
